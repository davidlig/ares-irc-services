# Email content — domain: mail (plain text, no IRC codes)
# Placeholders: %nickname%, %token%, %new_email%

# REGISTER / VERIFY — verification token
register_verification_subject: "%bot%: verify your nickname registration"
register_verification_body: |
  Hello,

  You requested to register the nickname "%nickname%" on the IRC network.

  Your verification token is: %token%

  To complete registration, send this command to %bot% (replace <token> with the token above):
    /msg %bot% VERIFY %token%

  The token expires in 1 hour. If you did not request this, please ignore this email.

# RESEND — same as register, resend of token
resend_verification_subject: "%bot%: your verification token (resend)"
resend_verification_body: |
  Hello,

  You requested a new verification token for the nickname "%nickname%".

  Your verification token is: %token%

  To complete registration, send to %bot%:
    /msg %bot% VERIFY %token%

  The token expires in 1 hour. If you did not request this, please ignore this email.

# SET EMAIL — confirmation token sent to current email
email_change_token_subject: "%bot%: confirm your new email address"
email_change_token_body: |
  Hello,

  You requested to change the email address for your %bot% nickname to: %new_email%

  Your confirmation token is: %token%

  To confirm the change, send to %bot%:
    /msg %bot% SET EMAIL %new_email% %token%

  The token expires in 1 hour. If you did not request this change, please ignore this email.

# RECOVER — password recovery token
recovery_token_subject: "%bot%: password recovery"
recovery_token_body: |
  Hello,

  A password recovery was requested for the nickname "%nickname%".

  Your recovery token is: %token%

  To reset your password, send to %bot%:
    /msg %bot% RECOVER %nickname% %token%

  The token expires in 1 hour. If you did not request this, please ignore this email.

# ChanServ SET FOUNDER — founder change token
founder_change_token_subject: "%bot%: founder change token for %channel%"
founder_change_token_body: |
  Hello,

  A founder change was requested for the channel "%channel%" to the nickname "%new_nick%".

  Your confirmation token is: %token%

  To complete the change, send to %bot% this command (you can copy and paste it):

    /msg %bot% %command%

  The token expires in 1 hour. If you did not request this, please ignore this email.
