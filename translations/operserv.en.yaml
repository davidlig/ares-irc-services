# OperServ translations — English
# Domain: operserv

unknown_command: "\x0304✗\x03 Unknown command \x02%command%\x02. Use \x0303/msg %bot% HELP\x03."

error:
  syntax:           "\x0304✗\x03 Incorrect syntax. Usage: \x02%syntax%\x02"
  oper_only:        "\x0304✗\x03 This command is restricted to IRC Operators."
  permission_denied: "\x0304✗\x03 Permission denied. You do not have access to this command."
  root_only:        "\x0304✗\x03 This command is restricted to network root administrators."
  nick_not_registered: "\x0304✗\x03 Nickname \x02%nick%\x02 is not registered."

# --- HELP --------------------------------------------------------------------
help:
  syntax:           "HELP [command [sub-option]]"
  short:            "List commands or show help for a command."
  help:             "Without arguments lists all commands. Use \x0303HELP <command>\x03 for full help, or \x0303HELP <command> <option>\x03 for sub-options."
  header_title:     "%bot%"
  intro:            "Network management service for IRC operators."
  general_header:   "\x02\x0314Available commands:\x03\x0F"
  command_line:     "  \x0307%command%\x03%description%"
  subcommand_line:  "  \x0307%command%\x03%description%"
  options_header:   "\x02\x0314Options:\x03\x0F"
  general_footer:   "Use \x0303/msg %bot% HELP <command>\x03 for more information."
  set_sub_footer:   "Use \x0303/msg %bot% HELP %command% <option>\x03 for more information."
  syntax_label:     "\x0307Syntax:\x03 \x02%syntax%\x02"
  footer:           "\x0314─────────────────────────────\x03"
  intro_expiration: "NOTE: Nicks \x0304\x1Funused\x0F for more than %days% days are automatically removed."
  unknown_command:  "\x0304✗\x03 Unknown command \x02%command%\x02. Use \x0303/msg %bot% HELP\x03."
  suboptions:       "Sub-options:"

# --- IRCOP --------------------------------------------------------------------
ircop:
  syntax:           "IRCOP <nickname> {ADD|DEL|LIST} <role>"
  short:            "Manage network IRC operators."
  help:             "Manage network IRC operators. ADD adds a registered nickname\nas an operator with the specified role.\nDEL removes an operator. LIST shows all operators.\nUse ROLE LIST to see available roles.\nThis command is restricted to Root users configured in services."

  unknown_sub:      "\x0304✗\x03 Unknown subcommand \x02%sub%\x02. Use ADD, DEL, or LIST."
  unknown_role:     "\x0304✗\x03 Role \x02%role%\x02 does not exist. Use \x0303/msg %bot% ROLE LIST\x03 to see available roles."
  already_admin:    "\x0304✗\x03 \x02%nick%\x02 is already an IRC operator (role: %role%)."
  not_admin:       "\x0304✗\x03 \x02%nick%\x02 is not an IRC operator."
  role_changed:     "\x0303✓\x03 Changed role for \x02%nick%\x02 from %old% to %new%."

  add:
    syntax:         "IRCOP <nickname> ADD <role>"
    short:          "Add a network IRC operator."
    help:           "Adds a registered nickname as a network IRC operator.\nUse ROLE LIST to see available roles.\nThis command is restricted to Root users."
    done:           "\x0303✓\x03 Added \x02%nick%\x02 as \x02%role%\x02."
    nick_not_active: "\x0304✗\x03 Nickname \x02%nick%\x02 is not an active registered nickname."

  del:
    syntax:         "IRCOP <nickname> DEL"
    short:          "Remove a network IRC operator."
    help:            "Removes a network IRC operator from the list.\nThis command is restricted to Root users."
    done:           "\x0303✓\x03 Removed \x02%nick%\x02 from IRC operators."

  list:
    syntax:         "IRCOP LIST"
    short:          "List all network IRC operators."
    help:            "Lists all network IRC operators with their roles and registration date.\nThis command is restricted to Root users."
    header:         "\x0307---\x03 \x02Network IRC Operators\x02 \x0307---\x03"
    empty:          "There are no IRC operators."

# --- ROLE --------------------------------------------------------------------
role:
  syntax:           "ROLE {ADD|DEL|LIST|PERMS|MODES|VHOST} <name> ..."
  short:            "Manage operator roles."
  help:             "Manage operator roles. ADD creates a new role, DEL removes it,\nLIST shows all roles, PERMS manages permissions for a role, MODES manages IRCOP modes,\nVHOST manages forced vhost pattern for a role.\nProtected roles (ADMIN, OPER, PREOPER) cannot be deleted.\nThis command is restricted to Root users."

  unknown_sub:      "\x0304✗\x03 Unknown subcommand \x02%sub%\x02. Use ADD, DEL, LIST, PERMS, MODES, or VHOST."
  not_found:        "\x0304✗\x03 Role \x02%role%\x02 does not exist."
  already_exists:   "\x0304✗\x03 Role \x02%role%\x02 already exists."
  protected:        "\x0304✗\x03 Role \x02%role%\x02 is protected and cannot be deleted."

  add:
    syntax:         "ROLE ADD <name> [description]"
    short:          "Create a new role."
    help:            "Creates a new custom role. Description is optional.\nThis command is restricted to Root users."
    done:           "\x0303✓\x03 Created role \x02%role%\x02."

  del:
    syntax:         "ROLE DEL <name>"
    short:          "Delete a role."
    help:            "Deletes a custom role. Protected roles (ADMIN, OPER, PREOPER)\ncannot be deleted. If the role has operators assigned,\nyou must reassign them first.\nThis command is restricted to Root users."
    done:           "\x0303✓\x03 Deleted role \x02%role%\x02."

  list:
    syntax:         "ROLE LIST"
    short:          "List all roles."
    help:            "Lists all roles. Protected roles are marked with [PROTECTED].\nThis command is restricted to Root users."
    header:         "\x0307---\x03 \x02Operator Roles\x02 \x0307---\x03"
    empty:          "There are no operator roles."

  perms:
    syntax:         "ROLE PERMS <role> {LIST|ADD|DEL} [permission]"
    short:          "Manage role permissions."
    help:            "Manage permissions assigned to a role.\nLIST shows all permissions for the role.\nADD assigns a permission to the role.\nDEL removes a permission from the role (not for protected roles).\nThis command is restricted to Root users."
    unknown_action:  "\x0304✗\x03 Unknown action \x02%action%\x02. Use LIST, ADD, or DEL."
    not_found:       "\x0304✗\x03 Permission \x02%perm%\x02 does not exist."
    already_has:     "\x0304✗\x03 Role \x02%role%\x02 already has permission \x02%perm%\x02."
    does_not_have:   "\x0304✗\x03 Role \x02%role%\x02 does not have permission \x02%perm%\x02."
    protected:      "\x0304✗\x03 Cannot modify permissions for protected role \x02%role%\x02."

    list:
      syntax:       "ROLE PERMS <role> LIST"
      short:        "List permissions for a role."
      help:         "Lists all permissions assigned to a role.\nThis command is restricted to Root users."
      header:        "\x0307---\x03 \x02Permissions for %role%\x02 \x0307---\x03"
      empty:        "Role \x02%role%\x02 has no permissions assigned."
      assigned:     "\x0310Assigned:\x03"
      none_assigned: "\x0314No permissions assigned.\x03"
      available:    "\x0310Available:\x03"
      all_assigned: "\x0314All permissions are assigned to this role.\x03"

    add:
      syntax:       "ROLE PERMS <role> ADD <permission>"
      short:        "Add a permission to a role."
      help:         "Adds a permission to a role.\nThis command is restricted to Root users."
      done:         "\x0303✓\x03 Added permission \x02%perm%\x02 to role \x02%role%\x02."

    del:
      syntax:       "ROLE PERMS <role> DEL <permission>"
      short:        "Remove a permission from a role."
      help:         "Removes a permission from a role.\nCannot modify protected roles.\nThis command is restricted to Root users."
      done:         "\x0303✓\x03 Removed permission \x02%perm%\x02 from role \x02%role%\x02."

  modes:
    syntax:         "ROLE MODES <role> {VIEW|SET} [modes]"
    short:          "Manage IRCOP user modes for the role."
    help:            "Manage IRCOP user modes assigned to a role.\nVIEW shows the role's current modes.\nSET sets the IRCOP modes (e.g., oH for +oH).\nUsers with this role will receive these modes on identify.\nAvailable modes per IRCD: UnrealIRCd (H, q, s, W), InspIRCd (s).\nThis command is restricted to Root users."
    unknown_action:  "\x0304✗\x03 Unknown action \x02%action%\x02. Use VIEW or SET."

    view:
      syntax:       "ROLE MODES <role> VIEW"
      short:        "Shows the role's modes."
      help:          "Shows the IRCOP user modes assigned to a role.\nThis command is restricted to Root users."
      header:        "\x0307---\x03 \x02Modes for %role%\x02 \x0307---\x03"
      empty:        "The role \x02%role%\x02 has no modes assigned."
      line:         "  Modes: \x0303%modes%\x03"

    set:
      syntax:       "ROLE MODES <role> SET [modes]"
      short:        "Set IRCOP user modes."
      help:          "Set the IRCOP user modes for the role.\nWithout arguments, removes all modes from the role.\nOnly valid IRCOP modes for the active IRCD.\nIdentified users with this role will receive the changes.\nThis command is restricted to Root users."
      done:         "\x0303✓\x03 Set modes \x02%modes%\x02 for role \x02%role%\x02."
      cleared:      "\x0303✓\x03 Removed all modes from role \x02%role%\x02."
      invalid_mode: "\x0304✗\x03 The mode \x02%mode%\x02 is not a valid IRCOP mode."
      invalid_modes: "\x0304✗\x03 Invalid modes: %invalid%. Valid modes: %valid%."
      no_irc_user_modes: "\x0304✗\x03 The current IRCD has no IRCOP modes configured."

  vhost:
    syntax:         "ROLE VHOST <role> {VIEW|SET} [pattern]"
    short:          "Manage forced vhost pattern for a role."
    help:           "Manage forced vhost assigned to a role.\nVIEW shows the current forced vhost pattern.\nSET assigns a forced vhost pattern (users with this role will\nreceive this vhost on identify, overriding personal vhost).\nThe pattern is appended after the cleaned nickname, e.g.:\n  Nick: _davidlig_ → Pattern: admin.ares → VHost: davidlig.admin.ares\nValid characters: a-z, A-Z, 0-9, hyphen (-), and dot (.).\nMust have at least one dot, cannot start/end with - or . or have ..\nUse OFF to clear the forced vhost pattern.\nThis command is restricted to Root users."
    unknown_action:  "\x0304✗\x03 Unknown action \x02%action%\x02. Use VIEW or SET."

    view:
      syntax:       "ROLE VHOST <role> VIEW"
      short:         "Shows the role's forced vhost pattern."
      help:          "Shows the forced vhost pattern assigned to a role.\nThis command is restricted to Root users."
      header:        "\x0307---\x03 \x02Forced vhost for %role%\x02 \x0307---\x03"
      empty:        "The role \x02%role%\x02 has no forced vhost pattern set."
      line:         "  Pattern: \x0303%pattern%\x03"
      example:      "  Example: \x02nickname\x02.%pattern%"

    set:
      syntax:       "ROLE VHOST <role> SET <pattern>"
      short:         "Set the forced vhost pattern for the role."
      help:          "Sets the forced vhost pattern for the role.\nUsers identifying with this role will receive the forced vhost,\nwhich takes priority over personal vhost.\nThe pattern is appended after the cleaned nickname.\nUse OFF or omit argument to clear the forced vhost.\nThis command is restricted to Root users."
      done:         "\x0303✓\x03 Set forced vhost pattern for role \x02%role%\x02."
      cleared:      "\x0303✓\x03 Removed forced vhost pattern from role \x02%role%\x02."
      invalid:      "\x0304✗\x03 Invalid vhost pattern. Valid characters: a-z, A-Z, 0-9, - and .. Must have at least one dot, cannot start/end with - or . or have .."

# --- KILL --------------------------------------------------------------------
kill:
  syntax:           "KILL <nickname> <reason>"
  short:            "Forcefully disconnect a user from the network."
  help:             "Disconnects a user from the IRC network.\nIRCops and Root users cannot be killed.\nThis command requires the operserv.kill permission.\n\nSyntax: KILL <nickname> <reason>\nExample: KILL BadUser Flooding channels"
  user_not_online:  "\x0304✗\x03 Nick \x02%nick%\x02 is not connected."
  protected_root:   "\x0304✗\x03 Cannot kill Root user \x02%nick%\x02."
  protected_ircop:  "\x0304✗\x03 Cannot kill IRC operator \x02%nick%\x02."
  done:             "\x0303✓\x03 User \x02%nick%\x02 has been killed (%reason%)."
  permission_denied: "\x0304✗\x03 Permission denied. You need operserv.kill permission."

# --- GLINE --------------------------------------------------------------------
gline:
  syntax:           "GLINE <ADD|DEL|LIST> [mask] [expiry] [reason]"
  short:            "Manage network-wide G-lines."
  help:             "Manage network-wide G-lines (global bans).\nADD adds a new G-line with mask, expiry and reason.\nDEL removes a G-line by mask or entry number.\nLIST shows all G-lines, optionally filtered by mask.\n\nExpiry format: 0=permanent, or use d, h, m suffix (e.g., 7d, 12h, 30m).\nGLINEs matching Root or IRCop users are rejected.\nThis command requires the operserv.gline permission."
  unknown_sub:      "\x0304✗\x03 Unknown subcommand \x02%sub%\x02. Use ADD, DEL, or LIST."
  invalid_mask:     "\x0304✗\x03 Invalid mask format. Must be in format user@host (e.g., *@192.168.*)."
  global_mask:      "\x0304✗\x03 G-line mask \x02%mask%\x02 is too broad. Global masks are not allowed."
  dangerous_mask:   "\x0304✗\x03 G-line mask \x02%mask%\x02 is too broad. Host part needs at least 4 alphanumeric characters."
  protected_user:   "\x0304✗\x03 Cannot G-line protected user \x02%nick%\x02."
  invalid_expiry:   "\x0304✗\x03 Invalid expiry format. Use 0 for permanent, or a value like 7d, 12h, 30m."
  already_exists:   "\x0304✗\x03 G-line for \x02%mask%\x02 already exists."
  max_entries:      "\x0304✗\x03 Maximum number of G-lines (%max%) reached."
  not_found:        "\x0304✗\x03 G-line \x02%mask%\x02 not found."
  user_not_found:   "\x0304✗\x03 User \x02%nick%\x02 is not online."
  permanent:        "permanent"

  add:
    syntax:         "GLINE ADD <mask> <expiry> <reason>"
    short:          "Add a network-wide G-line."
    help:           "Adds a network-wide G-line (global ban).\nThe mask must be in format user@host.\nExpiry: 0=permanent, or use d, h, m suffix (e.g., 7d, 12h, 30m).\nGLINEs matching Root or IRCop users are rejected.\nThis command requires the operserv.gline permission."
    done:           "\x0303✓\x03 G-line added: \x02%mask%\x02 (%duration%) - %reason%"

  del:
    syntax:         "GLINE DEL <mask|number>"
    short:          "Remove a G-line."
    help:           "Removes a G-line from the list.\nYou can specify the mask or the entry number from LIST.\nThis command requires the operserv.gline permission."
    done:           "\x0303✓\x03 G-line \x02%mask%\x02 removed."

  list:
    syntax:         "GLINE LIST [mask]"
    short:          "List all G-lines."
    help:           "Lists all G-lines. Optionally filter by mask pattern.\nThis command requires the operserv.gline permission."
    header:         "\x0307---\x03 \x02G-lines (%count%) \x0307---\x03"
    empty:          "There are no G-lines."
    no_reason:      "No reason"
    never_expires:  "Permanent"
    unknown_creator: "unknown"
    entry:          "\x0310%index%.\x03 \x02%mask%\x02 - %reason% (\x0314%nick%\x03, expires: %expiration%)"

# --- GLOBAL -------------------------------------------------------------------
global:
  syntax:           "GLOBAL {service|nick!ident@vhost} {PRIVMSG|NOTICE} <message>"
  short:            "Send a global message to all connected users."
  help:             "Sends a message to all connected users.\n\nIf the first argument is a service name (NickServ, ChanServ,\nMemoServ, OperServ), the message will be sent using that existing service.\n\nIf not a service, a temporary pseudo-client will be created with the\nspecified mask. The mask must be in format: nick!ident@vhost\n- nick: IRC valid nickname (letters, []{}|\\`, digits, starts with letter)\n- ident: alphanumeric with . _ ~ -\n- vhost: valid hostname\n\nFor pseudo-clients, the nickname cannot be connected or registered.\nThe pseudo-client will remain connected during the send and disconnect\nautomatically (the nick will be blocked for 24h via SQLINE/QLINE).\n\nPRIVMSG sends as private message, NOTICE sends as notice.\nThis command requires the operserv.global permission."
  mask_invalid:    "\x0304✗\x03 Invalid mask format: %error%"
  nick_service:    "\x0304✗\x03 Cannot use a service nickname."
  nick_connected: "\x0304✗\x03 Nickname \x02%nick%\x02 is currently connected."
  nick_registered: "\x0304✗\x03 Nickname \x02%nick%\x02 is registered."
  type_invalid:    "\x0304✗\x03 Message type must be PRIVMSG or NOTICE."
  done:            "\x0303✓\x03 Global message sent from \x02%nick%\x02 to %count% users."

# --- DEBUG -------------------------------------------------------------------
debug:
  action_message:   "%operator% executes command %command% on %target%. %reason%"
  actionWithDuration: "%operator% executes command %command% on %target%. Duration: %duration%. %reason%"
  prefix_reason:    "Reason: %reason%"
  prefix_message:   "Message: %reason%"

# --- PERMISSIONS --------------------------------------------------------------
permissions:
  operserv.kill:    "Disconnect a user from the network (KILL)"
  operserv.gline:   "Manage network-wide G-lines (GLINE)"
  operserv.global:  "Send global messages to all users (GLOBAL)"