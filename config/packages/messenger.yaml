framework:
    messenger:
        # Uncomment this (and the failed transport below) to send failed messages to this transport for later handling.
        # failure_transport: failed

        transports:
            sync: 'sync://'
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            # Dedicated queue for emails (delay is applied via middleware)
            async_emails: 'doctrine://default?queue_name=emails&auto_setup=0'

        routing:
            App\Bootstrap\Maintenance\Message\RunMaintenanceCycle: sync
            App\Shared\Application\Mail\Message\SendEmail: async_emails
            App\NickServ\Adapter\Out\Mail\RegistrationVerificationEmail: async_emails
            App\ChanServ\Adapter\Out\Mail\FounderChangeEmail: async_emails

        buses:
            default:
                middleware:
                    - App\Bootstrap\Adapter\Out\Messenger\Middleware\EmailDelayMiddleware

# when@test:
#    framework:
#        messenger:
#            transports:
#                # replace with your transport name here (e.g., my_transport: 'in-memory://')
#                # For more Messenger testing tools, see https://github.com/zenstruck/messenger-test
#                async: 'in-memory://'
